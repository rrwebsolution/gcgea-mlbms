import { CollectionReportPage } from "./CollectionReportPage"

export default function MonthlyCollectionReportPage() {
  return (
    <CollectionReportPage
      granularity="month"
      title="Monthly Collection Report"
      description="Track total collections — contributions and loan payments — per month."
    />
  )
}
