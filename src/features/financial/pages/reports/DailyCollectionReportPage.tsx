import { CollectionReportPage } from "./CollectionReportPage"

export default function DailyCollectionReportPage() {
  return (
    <CollectionReportPage
      granularity="day"
      title="Daily Collection Report"
      description="Track total collections — contributions and loan payments — per day."
    />
  )
}
