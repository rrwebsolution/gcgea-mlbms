import { CollectionReportPage } from "./CollectionReportPage"

export default function AnnualCollectionReportPage() {
  return (
    <CollectionReportPage
      granularity="year"
      title="Annual Collection Report"
      description="Track total collections — contributions and loan payments — per year."
    />
  )
}
